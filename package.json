{
  "name": "ai-autoreply-monorepo",
  "version": "6.0.0",
  "private": true,
  "description": "Chrome extension + separate license API (deploy server/ to Vercel)",
  "scripts": {
    "start": "npm --prefix server start",
    "server": "npm --prefix server start",
    "server:install": "npm --prefix server install"
  }
}
