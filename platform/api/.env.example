# Optional local notes only — API settings are read from app/config.py (not this file).
# Keep secrets out of git; prefer editing platform/api/app/config.py for this project setup.

# Seq logging is read directly from the process environment.
SEQ_URL=http://localhost:5341
# SEQ_API_KEY=
# SEQ_ENABLED=1
